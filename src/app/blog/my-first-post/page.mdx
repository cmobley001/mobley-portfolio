import { useMDXComponents } from '@/mdx-components';

# My First Blog Post

This is a paragraph written in **Markdown**. DaisyUI will automatically style it because we will wrap it in the `prose` class.

- List item 1
- List item 2

<button className="btn btn-primary">I can even use DaisyUI buttons here!</button>
